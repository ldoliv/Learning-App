
How to add new routes:

For the 1st level, being src/components/react/[folder]/index.js

index.js is required if we want it to fetch the description from routes.js from the subfolders
otherwise, edit reactRoutes.js directly and add the description