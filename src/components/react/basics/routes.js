

export const routes = [
	{
		path: 'update',
		label: <>
			<p>Show that by default a child component will always render when a parent component updates<br />
				In a child component, run useEffect when a prop value changes
			</p>
		</>,
		componentFilename: '1-update/Update'
	}
];
